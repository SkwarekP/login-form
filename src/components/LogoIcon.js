import IconLogo from "../../src/images/logo-icon.png"
import {Image} from "react-bootstrap";

function LogoIcon() {
    return (
        <Image
            src={IconLogo}
        />
    );
}

export default LogoIcon;
